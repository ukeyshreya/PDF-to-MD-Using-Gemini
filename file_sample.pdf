import PyPDF2
import os

def extract_text_from_pdf(sample.pdf1):
    with open(sample.pdf1, 'rb') as file:
        reader = PyPDF2.PdfReader(file)
        text = ''
        for page in reader.pages:
            text += page.extract_text() + '\n'
    return text

if __name__ == '__main__':
    pdf_file = '../file-sample.pdf'  # use correct path if inside src
    if os.path.exists(sample.pdf1.pdf):
        text = extract_text_from_pdf(sample.pdf1.pdf)
        print(text)
    else:
        print("❌ PDF file not found. Place 'file-sample.pdf' in project folder.")
